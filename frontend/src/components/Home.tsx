export const Home = () => {
  return (
    <div>
      <h1>Document Search with Embeddings</h1>
      <p>
        This is a simple document search application that uses embeddings to
        search for similar documents.
      </p>
    </div>
  );
};

export default Home;